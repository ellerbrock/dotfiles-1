" enable syntax highlighting
syntax enable

" show line numbers
set number

" set tabs to have 4 spaces
set ts=4

" indent when moving to the next line
set autoindent

" expand tabs into spaces
set expandtab

" shift lines by 4 spaces
set shiftwidth=4

" show matching part of pair for brackets
set showmatch

" enable python highlighting
let python_highlight_all=1
